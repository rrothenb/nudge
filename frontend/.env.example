# Nudge Frontend Environment Configuration

# =============================================================================
# API Backend URL
# =============================================================================

# Option 1: Local Development (Recommended for testing)
# Use the local mock backend server
VITE_API_URL=http://localhost:3000

# Option 2: AWS Deployment (Production)
# Update this after running `sam deploy` - get URL from stack outputs
# VITE_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/dev

# =============================================================================
# AWS Cognito Configuration (Production Only)
# =============================================================================
# Update these values after deploying with SAM
# Get these from CloudFormation stack outputs after deployment

VITE_AWS_REGION=us-east-1
# VITE_USER_POOL_ID=us-east-1_XXXXXXXXX
# VITE_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Optional Configuration
# =============================================================================

# Environment name for debugging
VITE_ENVIRONMENT=development

# =============================================================================
# Quick Start
# =============================================================================
#
# For local development:
# 1. Copy this file to .env: cp .env.example .env
# 2. Start local backend: cd ../backend/local-server && npm run dev
# 3. Start frontend: npm run dev
# 4. Open http://localhost:5173
#
# For AWS deployment:
# 1. Deploy backend: sam build && sam deploy --guided
# 2. Copy API endpoint from outputs
# 3. Update VITE_API_URL above with your endpoint
# 4. Update Cognito values from outputs
# 5. Build frontend: npm run build
# 6. Deploy to S3: aws s3 sync dist/ s3://your-bucket-name/
